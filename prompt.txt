Choose option 1, but DO NOT implement everything at once.

First fix only the critical security and financial-data foundation.

IMPORTANT:
- Inspect the existing code before changing it.
- Do not rewrite working features unnecessarily.
- Do not break existing CSV import, AI categorization, receipt processing, AI agent, budgets, anomalies, forecasting, or dashboard functionality.
- Run the existing backend tests after changes.
- Add tests for every security/foundation change.
- Do not expose or print API keys/secrets in logs or output.

Implement these in this order:

1. API KEY / SECRET SECURITY
- Remove any real API key from tracked/project files.
- Do NOT print the existing key.
- Change configuration to use OPENROUTER_API_KEY.
- Remove the confusing GEMINI_API_KEY fallback.
- Make missing API keys fail safely with a clear server-side error.
- Check .gitignore and Docker configuration.
- Add a .dockerignore so .env, database files, virtual environments, uploads, caches, and secrets are not copied into Docker images.
- If git history contains the secret, clearly tell me that it must be rotated/revoked and removed from git history. Do not attempt destructive git-history rewriting automatically.

2. FILE UPLOAD SECURITY
For receipt uploads:
- Validate actual file type/content, not only Content-Type.
- Add a reasonable maximum file size.
- Generate server-side random filenames.
- Never use the user-provided filename directly as a filesystem path.
- Store uploads outside publicly served directories where appropriate.
- Reject unsupported image formats.
- Make sure malformed files cannot crash the application.
- Add tests for invalid file types, oversized files, and malicious filenames.

For CSV uploads:
- Add file-size limits.
- Validate that the uploaded content is actually a readable CSV.
- Handle malformed CSV files gracefully.

3. FINANCIAL DATA CORRECTNESS
- Inspect how Transaction.amount is currently represented.
- Replace Float with Decimal/Numeric suitable for currency.
- Update calculations, schemas, serializers, AI tools, forecasting, budgets, anomalies, and what-if calculations accordingly.
- Preserve existing API behavior where possible.
- Add tests proving monetary calculations remain accurate.

4. ERROR HANDLING
- Do not swallow exceptions silently.
- Give the frontend safe, useful error messages.
- Keep detailed errors server-side only.
- Add appropriate timeouts for external AI requests.
- Do not expose API keys or internal stack traces.

5. RATE LIMITING / ABUSE PROTECTION
- Protect the AI endpoint from unlimited requests if practical within the current architecture.
- If a proper rate-limiter dependency is needed, explain the choice before adding unnecessary dependencies.
- At minimum, prevent obviously unlimited AI API abuse.

6. TESTING
Run the existing tests.

Add tests covering:
- Secret/config loading
- Decimal money calculations
- Receipt upload validation
- CSV upload validation
- Path traversal/malicious filenames
- AI API failure handling
- Existing functionality regression

7. FINAL REPORT
After implementation, report:
- Files changed
- What was fixed
- Tests added
- Test results
- Any remaining security risks
- Any breaking changes
- Anything that requires manual action from me

DO NOT:
- Implement authentication yet
- Migrate to PostgreSQL yet
- Add new UI features yet
- Add merchant search yet
- Add dashboard features yet
- Implement unrelated improvements

The goal of this task is to make the existing application safer and financially reliable without changing its product scope.