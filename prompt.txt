Implement ONLY Phase 2B: User Scoping + Database Migration.

Tasks:

* Add user_id foreign keys to all user-owned models.
* Add appropriate user_id indexes.
* Add user_id filtering to ALL CRUD queries, routers, and services.
* Scope all AI agent tools to the authenticated user's user_id.
* Ensure users cannot access another user's resources by changing an ID.
* Set up Alembic migrations.
* Safely migrate existing database data using the default-user/backfill strategy from PHASE2_PLAN.md.
* Remove manual schema ALTER logic from main.py.

IMPORTANT:

* Never accept user_id from the request body/query as the authority. Always derive it from the authenticated JWT.
* Test cross-user access explicitly for transactions, budgets, goals, receipts, anomalies, and agent queries.
* Back up the database before migration.
* Verify row counts and data preservation after migration.
* Do not implement frontend changes or transaction editing/search yet.

Run the complete backend test suite and report:

1. migration result
2. data-preservation numbers
3. cross-user isolation test results
4. total tests passed

Stop after Phase 2B.
