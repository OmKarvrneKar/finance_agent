Implement ONLY Phase 2A: Authentication Foundation from PHASE2_PLAN.md.

Implement:

* User model
* password hashing
* JWT creation/verification
* get_current_user dependency
* register/login/me endpoints
* required auth schemas
* JWT environment configuration

Do NOT implement user_id scoping, migrations, frontend auth, or transaction features yet.

Requirements:

* never log passwords or JWTs
* validate duplicate emails
* secure password hashing
* JWT secret must come from environment
* expired/invalid tokens must be rejected
* keep existing Phase 1 functionality working

Add backend tests for registration, login, wrong password, invalid token, expired token, and /me.

Run the full backend test suite.

Report exactly what changed and test results. Do not start Phase 2B.
